# Work directories (session-specific, not committed)
work/*/

# Keep the WORK-TO-PR.md file
!work/WORK-TO-PR.md

# Human-read-only directory (baseline repos for inspection — agents must NEVER edit)
human-read-only/

# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~
.idea/
.vscode/settings.json

# Environment files
.env
.env.local
.env.*.local

# Node modules (if any utilities use them)
node_modules/
